Tu as raison sur l’objectif (un seul backend), mais ta phrase laisse un angle mort : **“Supabase n’est plus utilisé”** n’est vrai que *dans ton intention*, pas forcément dans le code tant que la migration n’est pas faite. Tu veux éviter qu’il réponde “ok” puis qu’il casse 2 écrans.

Voici une version plus “cadrée” (tu peux lui renvoyer), avec des exigences testables et une méthode qui limite les régressions :

---

### Message amélioré à lui renvoyer

Oui, on doit **sortir Supabase complètement** : `MenuService.js` ne doit plus servir en prod.
Je veux donc :

1. **Remplacer `MenuService.js` par `ApiService.js` partout** (aucun import Supabase restant dans les pages/containers).
2. **Supprimer `MenuService.js`** à la fin **uniquement quand** tout fonctionne (sinon on garde temporairement mais “dead code” interdit en prod).
3. Garantir **zéro régression fonctionnelle** : mêmes écrans, mêmes actions, mêmes retours utilisateur (viewer/admin, CRUD menus/plats, édition semaine/jour).
4. Me fournir une **checklist de tests** + une **liste des fichiers modifiés** (idéalement un PR) :

   * lecture menu semaine
   * lecture menu jour
   * édition / ajout / suppression
   * auth admin (JWT)
   * erreurs API (401/403/404) gérées proprement

Et surtout : si certaines routes API manquent côté Express/MySQL, **ajoute-les proprement** (dans Express) plutôt que de bricoler côté front.

---

### Pourquoi c’est mieux (coaching direct)

* Tu le forces à livrer un résultat **vérifiable**, pas “j’ai migré”.
* Tu évites le piège “on supprime MenuService.js puis on découvre qu’une feature dépendait encore de Supabase”.
* Tu imposes une migration **contrat-first** : si l’API n’a pas les endpoints nécessaires, on complète l’API plutôt que d’avoir un front incohérent.

---

### Petit détail technique à ajouter (utile)

Demande aussi qu’il te donne un grep final :

```bash
grep -R "supabase" -n src server || true
```

👉 Objectif : **zéro occurrence** (ou seulement dans docs si tu veux).

---

Si tu veux, colle ici le contenu de `src/services/ApiService.js` (et éventuellement `MenuService.js`) et je te dis **exactement** quelles méthodes doivent exister côté API pour couvrir `WeekMenuPage`, `DailyMenu`, `WeekEditor`, etc., sans casser l’app.
